<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="OperationPrime.Presentation.Views.PlaceholderView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <StackPanel HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   Spacing="16">
            
            <FontIcon Glyph="&#xE783;" 
                     FontSize="48" 
                     Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            
            <TextBlock Text="{x:Bind ViewModel.FeatureName, Mode=OneWay}" 
                       Style="{StaticResource TitleTextBlockStyle}"
                       HorizontalAlignment="Center"/>
            
            <TextBlock Text="{x:Bind ViewModel.Message, Mode=OneWay}" 
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"
                       MaxWidth="400"/>
            
            <Button Content="Back to Dashboard" 
                   Style="{StaticResource AccentButtonStyle}"
                   Command="{x:Bind ViewModel.BackToDashboardCommand}"
                   Margin="0,16,0,0"/>
            
        </StackPanel>
    </Grid>
</Page>
