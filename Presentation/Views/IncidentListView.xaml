<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="OperationPrime.Presentation.Views.IncidentListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:converters="using:OperationPrime.Presentation.Converters"
      xmlns:domain="using:OperationPrime.Domain.Entities">

    <!-- Converters are registered globally in App.xaml -->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="20,20,20,20" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Spacing="20">
                <TextBlock Text="Incident Management" 
                           Style="{StaticResource TitleTextBlockStyle}"/>
                
                <!-- Error Message -->
                <InfoBar IsOpen="{x:Bind ViewModel.ErrorMessage, Converter={StaticResource StringToBoolConverter}, Mode=OneWay}"
                         Severity="Error"
                         Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                         Visibility="{x:Bind ViewModel.ErrorMessage, Converter={StaticResource StringToVisibilityConverter}, Mode=OneWay}"/>
                
                <!-- Success Message -->
                <InfoBar IsOpen="{x:Bind ViewModel.SuccessMessage, Converter={StaticResource StringToBoolConverter}, Mode=OneWay}"
                         Severity="Success"
                         Message="{x:Bind ViewModel.SuccessMessage, Mode=OneWay}"
                         Visibility="{x:Bind ViewModel.SuccessMessage, Converter={StaticResource StringToVisibilityConverter}, Mode=OneWay}"/>
            </StackPanel>
            
            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10" VerticalAlignment="Top">
                <Button Command="{x:Bind ViewModel.CreateIncidentCommand}"
                        ToolTipService.ToolTip="Create new incident"
                        Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Add"/>
                        <TextBlock Text="Create Incident"/>
                    </StackPanel>
                </Button>
                
                <Button Command="{x:Bind ViewModel.RefreshCommand}"
                        ToolTipService.ToolTip="Refresh incidents">
                    <SymbolIcon Symbol="Refresh"/>
                </Button>
                
                <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                              Width="20" Height="20"
                              Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}"/>
            </StackPanel>
        </Grid>

        <!-- Incidents List -->
        <ListView Grid.Row="1" 
                  ItemsSource="{x:Bind ViewModel.Incidents, Mode=OneWay}"
                  Margin="20,10,20,20"
                  SelectionMode="Single">
            
            <ListView.Header>
                <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                      Padding="16,12"
                      CornerRadius="4,4,0,0"
                      BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                      BorderThickness="1,1,1,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1.5*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Title" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="Type" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="Priority" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="3" Text="Status" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="4" Text="Created" FontWeight="SemiBold"/>
                </Grid>
            </ListView.Header>
            
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
            
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="domain:Incident">
                    <Grid Padding="16,12"
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          CornerRadius="0,0,4,4"
                          BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                          BorderThickness="1,0,1,1"
                          Margin="0,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1.5*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{x:Bind Title}" 
                                       FontWeight="Medium"
                                       TextWrapping="Wrap"/>
                            <TextBlock Text="{x:Bind Description}" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       FontSize="12"
                                       TextWrapping="Wrap"
                                       MaxLines="2"/>
                        </StackPanel>
                        
                        <TextBlock Grid.Column="1" 
                                   Text="{x:Bind IncidentType, Converter={StaticResource EnumToStringConverter}}"
                                   VerticalAlignment="Center"/>
                        
                        <TextBlock Grid.Column="2" 
                                   Text="{x:Bind Priority, Converter={StaticResource EnumToStringConverter}}"
                                   VerticalAlignment="Center"/>
                        
                        <TextBlock Grid.Column="3" 
                                   Text="{x:Bind Status, Converter={StaticResource EnumToStringConverter}}"
                                   VerticalAlignment="Center"/>
                        
                        <TextBlock Grid.Column="4" 
                                   Text="{x:Bind CreatedDate, Converter={StaticResource DateTimeToStringConverter}}"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Status Bar -->
        <Border Grid.Row="2" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                Padding="20,10">
            <TextBlock Text="{x:Bind ViewModel.Incidents.Count, Mode=OneWay}" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Border>
    </Grid>
</Page>
